import React from 'react';

import {Box} from '@common'

import * as styles from './HeaderStyles'

export const Header = () => {
    return (
        <Box sx={styles.Header}>
            Header
        </Box>
    );
};